name: Build Docker Image
description: 
inputs:
  replace_with_your_input_name:
    description: "Replace-With-Your-Input-Description"
    required: false
    default: "Replace-With-Your-Input-Default-Value"
runs:
  using: composite
  steps:
    - run: |
        docker build . --file Dockerfile --tag academy:$(date +%s)
      shell: bash